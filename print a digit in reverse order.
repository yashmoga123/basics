#include <bits/stdc++.h>
using namespace std;

int main() {
	
	int n;
	cin>>n;
	int m = n;
	
	int reverse = 0;
	
	while( n > 0){
		
		reverse = reverse * 10 +  n % 10;
		n =n/10;
	}
	cout<<" the reverse of "<<m<<" is "<<reverse<<endl;
	
	
	
	
	return 0;
}
