#include <bits/stdc++.h>
using namespace std;

int main() {

    char ch;
    cin>>ch;
    
    if( ch>='a' && ch<='z'){
    	
    	cout<<" it is lowercase ";
    }
    else  if( ch>='A' && ch<='Z'){
    	
    	cout<<" it is uppercase ";
    }



	return 0;
}
