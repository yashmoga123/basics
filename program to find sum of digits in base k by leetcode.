class Solution {
public:
    int sumBase(int n, int k) {
        
        int sum = 0;

        while( n!=0){

            int digits = n % k;
            sum += digits;
            n = n/k;
        }
        return sum;
    }
};
